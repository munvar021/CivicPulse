use font awesome icons only
use react hook form only
make it responsive for all devices including mobile devices, tablet and desktop. for this use media queries.
improve border radius and remaining things also.
improve the ui.
add smooth animation for every section.
in password section, implement the password view option also. so that user can see inside the password field.
maintain the same consistant ui and theme in the whole project.
add validation for all the fields using react hook form.
for email field, add validation for proper email format.
for password field, add validation for minimum 8 characters, at least one uppercase letter, one special character, one lower case, one number. use regex.
remove the alert. instead of alert, use toast or popup. which suits best for that scenario. make it responsive.
add a success message when the form is submitted successfully. use toast or popup for that as well.
add error messages for each field when validation fails. display these messages below the respective fields.
create a loading spinnner, for this entire project.
add the loading spinner to the submit button when the form is being submitted. disable the button while loading.
make the code resubale.
if there is no data, show message. if it is a table, hide the table and then show the message.
add pagination for tables if there are more than 10 entries. make it responsive. and also implement the pagination from the backend.
add sorting functionality to the table columns. allow users to sort the data in ascending or descending order by clicking on the column headers.
add a search functionality to the table. allow users to search for specific entries based on keywords. implement it from the backend.
implement the scroll based animation for every section.
merge the scroll based animations with the smooth animations and the animations that we are created in animations filex for every section.
improve the spacing like margin and padding.
add a consistent color scheme throughout the project. choose colors that complement each other and enhance the overall user experience.
use react select for the dropdowns and text search functionality.
add a clear button for the search functionality to allow users to easily reset their search criteria.
dont use inline stylings, used styled components only
